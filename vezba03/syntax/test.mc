int abs(int i) {
  int res;
  if(i < 0 && a < 0 && b > 0)
    res = 0 - i;
  else 
    res = i;
  return res;
}

int main() {
    int a;
    int b, c;
    
    for i = 1 to 9
     a = a + 1;
    next i

    for j = 10 downto 0 step 2
     a = a - 1;
    next j

    do
    {
	a = 5;
    }while(a < 5);
    do
        a++;
    while(a < 5);

  return abs(-5);
}
